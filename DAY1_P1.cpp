#include <fstream>
#include <iostream>
#include <string>
using namespace std;
ifstream data("input.txt");
string mytext;
long int add, sum, bigt;
int main() {
  while (getline(data, mytext)) {
    if (mytext==""){

    }else{
      add=stoi(mytext);
      sum+=add
;    }
  }
  cout<<bigt;
}
